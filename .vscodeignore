.git/**
.gitignore
.github/**
.vscode/**
.vscode-test/**
node_modules/**
*.vsix
.DS_Store
**/.DS_Store
*.log
npm-debug.log*
yarn-error.log*

# Build tooling and inputs — the generated themes/ are what ship, not the
# generator or the palette source.
scripts/**
palettes/**
package-lock.json

# Preview renders are for the README on GitHub, which loads them over https.
# Shipping ~1.4 MB of screenshots inside the .vsix would be dead weight.
images/previews/**
images/hero.png
images/icon.svg
